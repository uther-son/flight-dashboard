# flight-dashboard — 운영 메모

항공권 특가 모니터링 대시보드. 일본 7개 도시 + 호주(시드니) 노선을 매일
자동 검색해서 최저가/추이/추천 여행일자를 보여준다. 이 문서는 2026-08-15
하루 동안 발견된 버그와 함정들을 정리한 것 — 같은 실수를 반복하지 않기 위함.

## 아키텍처 — 검색은 반드시 `/api/search` 하나로

- 대시보드 "지금 조회하기" 버튼(POST) / Claude Code 루틴 "FlightAlertDaily"
  (GET, 매일 10:30 KST) 둘 다 이 엔드포인트 하나만 호출한다.
- **절대 별도의 검색 로직을 다른 곳에 새로 만들지 말 것.** 예전엔 버튼·
  Vercel Cron·루틴(인라인 Python)이 각자 다른 코드로 검색해서 노선 목록·
  결과 형식이 서로 어긋나는 버그가 반복됐다. 지금은 전부 걷어내고 하나로
  통일한 상태 — 새 기능이 필요하면 이 엔드포인트/`lib/myrealtrip.ts`를
  확장하지, 옆에 새로 만들지 않는다.
- 루틴은 클라우드 샌드박스라 `mcp-servers.myrealtrip.com`과 이 대시보드
  도메인에 raw fetch/curl이 네트워크 정책으로 막혀 있다. 반드시 정식 MCP
  커넥터로만 통신: 검색 트리거는 Vercel 커넥터의 `web_fetch_vercel_url`
  (GET 전용이라 버튼은 POST, 루틴은 GET), 캘린더는 Google-Calendar 커넥터,
  알림은 Gmail 커넥터. "직접 fetch 안 되면 그냥 안 되는 거겠지"라고 넘기지
  말고 커넥터 경유 방법부터 찾을 것.
- 일본은 fare calendar로 도시별 최저가 "날짜"까지 자동 탐색(고정 날짜
  아님), 호주(시드니)도 동일한 방식으로 오늘부터 6개월 이내 최저가
  자동 탐색(체류 20박만 고정). 사용자가 "OO월~OO월 사이"처럼 기간을
  주면, 그게 고정 출발일 지정인지 그 범위 안에서 최저가를 자동으로
  찾으라는 뜻인지 헷갈리지 말 것 — 애매하면 물어볼 것 (오늘 시드니
  작업에서 처음에 고정 날짜로 잘못 짰다가 다시 만들었음).

## 배포

- Vercel 프로젝트가 GitHub 저장소와 연결되어 있어 `master`에 push하면
  자동 배포된다. 예전엔 GitHub App 설치가 안 되어 있어서 push해도 배포가
  전혀 안 되고 있었다(수 주간 눈치 못 챔) — 배포가 안 되는 것 같으면
  `vercel.com/aidenson-s-projects/flight-dashboard/settings/git`에서
  연동 상태부터 확인.
- (연결이 끊겼을 경우에만) Vercel MCP 커넥터의 `deploy_to_vercel`로 직접
  배포 가능 — 이땐 커밋 메타데이터 없이 배포되니 정상화되면 다시 git
  연동을 확인할 것.
- VS Code 안에서 "커밋" 눌렀는데 파이프 에러(`vscode-git-...-sock`)가 나면
  코드 문제가 아니라 VS Code 자체 재시작으로 해결되는 경우가 많음(특히
  터미널로 동시에 git 명령을 실행하고 있었을 때).

## 타임존 — 반드시 KST 명시

- Vercel 서버는 UTC로 돈다. 사용자에게 보여주는 모든 날짜/시각은:
  - `toLocaleDateString`/`toLocaleString`에 **`timeZone: 'Asia/Seoul'`을
    항상 명시**할 것. 빠뜨리면 실제 시각보다 9시간 이른 UTC가 그대로
    노출된다 — 실제로 대시보드 헤더가 "오전 5시"로 잘못 뜬 채 한동안
    있었다(실제로는 오후 2시).
  - 요일/일수 계산(`getDay`, `setDate` 등)은 로컬 타임존 getter 대신
    UTC getter(`getUTCDay`, `setUTCDate`)로 통일하고, "YYYY-MM-DD" 문자열은
    항상 `new Date(\`${s}T00:00:00Z\`)`처럼 자정 UTC로 파싱할 것. 문자열에
    `+09:00`을 직접 섞어 파싱하면(예: `new Date(s + 'T00:00:00+09:00')`)
    자정 부근에서 요일이 하루 밀리는 버그가 생긴다 — 실제로 토요일을
    금요일로 잘못 계산해서 주말 추천 로직이 틀어졌었다. 날짜 관련 코드를
    새로 짜거나 고칠 땐 이 패턴부터 의심할 것.

## 데이터 (Upstash Redis)

- `flight_results`: 대시보드에 보여주는 최신 검색 결과 1건.
- `flight_history`: 가격 추이용 일별 기록. **도착 공항 코드 하나로만
  키를 잡는다** (예: `FUK`, `SYD`) — 출발공항(인천/김포)이나 날짜를 키에
  섞으면 같은 도시인데 출발지·날짜가 바뀔 때마다 추이 탭이 쪼개지는
  버그가 생긴다(오사카=인천/김포 두 노선 경쟁, 시드니=매번 다른 최저가
  날짜로 실제로 겪었음 — 뉴질랜드 시절엔 탭이 3개로 쪼개지고 라벨도
  전부 "뉴질랜드"로만 나와 구분이 안 됐었다).
- 저장 스키마(키 형식)를 바꿀 땐 예전 형식의 레코드가 고아로 남아 화면에
  이상한 탭/유령 데이터로 나타난다(실제로 "ICN→TYO"라는 존재하지도 않는
  옛날 실험 잔재가 몇 달째 떠 있었다). 스키마를 바꾸면 임시 라우트로
  `flight_history`를 `{}`로 초기화한 뒤 정리하고, 임시 라우트는 확인 후
  바로 제거할 것.
- 저장하는 쪽(`saveResults`/`updateHistory`)이 항상 정규화된 형태로만
  쓰기 때문에, 읽는 쪽에서 "혹시 모를 이상한 형식"을 되살리는 방어
  코드는 필요 없다. 과거에 루틴이 제각각 포맷으로 보내던 시절의 복구
  로직(`normalizeDeal`, `parseRouteCodes` 등)이 남아있었는데 전부
  죽은 코드였음 — 새 방어 로직을 추가하기 전에 "지금 이 데이터가 정말
  다른 형식으로 들어올 수 있는가"부터 확인할 것.

## 외부 서비스 한계

- myrealtrip MCP는 실제로 요청 제한(429)이 있다. 짧은 세션에서 반복
  테스트하면 금방 걸린다 — 검색 API 자체에 완전 실패 시 3초 대기 후
  1회 재시도 로직이 있음(`/api/search`). 디버깅/테스트 중엔 호출
  간격을 두고, 결과가 비어있으면 코드 버그인지 그냥 순간 요청제한인지
  먼저 구분할 것(재시도해서 되면 후자).
- 일본(도시 7개, 병렬 8개 요청)과 호주(단일 요청 몇 개)를 동시에 돌리면
  호주 쪽 요청이 일본의 초기 병렬 폭주에 묻혀 타임아웃 나는 경우가
  있었다 — 지금은 호주를 먼저 순차 실행하도록 순서를 바꿔서 해결.
  비슷하게 여러 외부 호출을 `Promise.all`로 동시에 묶을 땐 "먼저 도는
  놈이 많으면 나중 놈이 굶을 수 있다"는 걸 염두에 둘 것.
- 공휴일은 Google이 기본 제공하는 "대한민국의 휴일" 캘린더
  (`ko.south_korea#holiday@group.v.calendar.google.com`)에서 가져온다.
  **하드코딩하지 말 것** — 예전에 하드코딩했을 때 2027년 설날 날짜를
  통째로 틀리게 넣은 적 있다(1/25로 넣었는데 실제는 2/7). `description`이
  정확히 "공휴일"인 것만 법정 공휴일이고, "기념일"(크리스마스이브,
  국군의날, 섣달그믐날 등)은 쉬는 날이 아니니 제외해야 한다. 자주 안
  바뀌는 데이터라 매일 새로 가져오지 않고 매월 1일에만 갱신, 평소엔
  대시보드에 저장된 값을 재사용한다(루틴 프롬프트 1-2단계 참고).

## 코드/UI를 건드릴 때 같이 확인할 것

- **안내 문구는 실제 동작과 같이 업데이트할 것.** 코드는 6개월 범위로
  검색하는데 화면·툴팁엔 "이번달~다음달(62일)"이라고 오래전부터 잘못
  적혀 있었다. 검색 로직을 바꾸면 그 아래 UI 문구도 반드시 같이 확인.
- **섹션/라벨 네이밍은 기존 패턴과 맞출 것.** "🇯🇵 일본 최저가"가
  있으면 호주 쪽도 "🇦🇺 호주(시드니) 최저가"처럼 통일 — "시드니"만
  덜렁 쓰면 패턴이 깨진다.
- **맥락 없이 붙어있는 UI 요소는 의심할 것.** 설명 없이 "3인 총액"
  같은 문구가 왜 있는지 모르게 붙어있던 사례가 있었다 — 존재 이유가
  불분명하면 지우거나 사용자에게 물어볼 것, 그냥 유지하지 말 것.
- **죽은 변수/import는 눈에 띄는 대로 정리.** 선언만 되고 안 쓰이는
  변수(`japanSpecials` 등), 안 쓰는 import가 쌓여 있었다.
- **직항/경유처럼 "둘 중 하나"인 상태는 항상 둘 다 표기.** 한쪽 조건일
  때만 배지를 보여주고 반대 조건일 땐 아무것도 안 보여주면, 화면상
  "이 항목만 정보가 없어 보이는" 착시가 생긴다(경유편에 아무 배지도
  없어서 뉴질랜드 카드만 유독 다르게 보였던 사례).
- 커밋하기 전에 디버그용으로 만든 스크래치 파일이 같이 올라가지 않는지
  `git status`로 확인할 것 — 한 번 실수로 커밋됐다가 바로 다음 커밋에서
  지운 적 있음.
